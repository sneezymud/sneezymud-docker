# SneezyMUD Docker Environment Configuration
# Copy this file to .env and configure as needed

# Discord Notifications (Optional)
# Set your Discord webhook URL to receive notifications when the game is
# updated via automatic updates
DISCORD_WEBHOOK_URL=

# Discord Crash Reports (Optional)
# Set a webhook URL to receive crash reports with relevant crash details attached
DISCORD_CRASH_WEBHOOK_URL=

# Backups API access via nginx (Optional)
#
# Set to enable the /sneezybackups/ API endpoint (when nginx
# is configured on the server via the init-nginx.sh script). This allows
# external tools to download backup files and daily change archives
#
# Generate a secure API key with: openssl rand -hex 32
# Use the generated API key here and in the external tool's configuration
#
# Make sure to run sudo ./scripts/nginx/init-nginx.sh --update-services
# after changing this value to update the nginx configuration
BACKUP_API_KEY=
