# Database connection (Docker DNS resolves sneezy-db to the MariaDB container)
DB_HOST=sneezy-db
DB_USER=sneezy
DB_PASS=password

# Session signing secret — change in production
SESSION_SECRET=change-me-in-production

# Port for the Hono API server (Vite proxies /api here in dev)
API_PORT=3001
